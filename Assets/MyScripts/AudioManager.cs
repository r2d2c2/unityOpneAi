using UnityEngine;
using System.Runtime.InteropServices;

public class AudioManager : MonoBehaviour
{
    /*[DllImport("__Internal")]
    private static extern void startRecording();

    void Start()
    {
        // JavaScript로 녹음 시작 명령을 보냄
        startRecording();
    }

    // JavaScript로부터 호출될 함수
    public void OnAudioRecorded(string audioData)
    {
        Debug.Log("Audio recorded: " + audioData);
        // 여기에 녹음된 데이터를 처리하는 로직 추가
    }*/
}
